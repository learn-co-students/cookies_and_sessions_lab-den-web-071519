
<%= form_tag('/', method: 'post') do %>
    <%= label_tag('product', "Add Item To Cart") %>
    <%= text_field_tag('product') %>
    <%= submit_tag("add to cart") %>
<% end %>

<%@cart = session[:cart] || []%>
<% @cart.each do |item| %>
    <%=item %>
<% end %>
